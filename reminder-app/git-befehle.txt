Navigieren in Ordnerstruktur:
	cd <Ordnername> --> navigieren in n�chste Ebene
	cd <vollst�ndiger Pfad> direktes navigieren in angegebenen Pfad
	cd .. --> eine Ebene zur�ck navigieren
	cd \ navigieren auf root
Befehle GIT:
	git clone <remote repos link>
	git add <was soll gestaget werden?>
		git add . --> alle �nderungen Stagen / lokal
	git commit -m "Text f�r Commit" / lokal
	git push --> in remote repos hinzuf�gen / global
	git pull --> Alle �nderungen aus dem remote repos mit meinem lokalen repos mergen
	git fetch --> �nderungen f�r dediziertes mergen vorbereiten und aus remote repos holen
Ionic-Befehle:
	npx ionic serve --> Echtzeit Vorschau der App
	npm run dev --> Instanz als Version der App bei Ausf�hrung

Install-Befehle:
npm install @capacitor/local-notifications
npm install @capacitor/preferences
npx cap sync
